apiVersion: apps/v1
kind: Deployment
metadata:
  name: friendsdrinks-meetup-writer-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: friendsdrinks-meetup-writer-service
  template:
    metadata:
      labels:
        app: friendsdrinks-meetup-writer-service
    spec:
      containers:
        - name: friendsdrinks-meetup-writer-service
          image: $img
          imagePullPolicy: Always
          volumeMounts:
            - name: config
              mountPath: /app/config
              readOnly: true
          livenessProbe:
            httpGet:
              path: /v1/health
              port: 8080
            initialDelaySeconds: 600
            timeoutSeconds: 2
            periodSeconds: 30
          ports:
            - containerPort: 8080
          args: ["andrewgrant.friendsdrinks.meetup.WriterService", "/app/config/dev-kubernetes.properties"]
      volumes:
        - name: config
          configMap:
            name: streams-config
